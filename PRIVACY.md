# 隐私说明

Compound Knowledge 是本地优先的 Markdown 技能包：不收集遥测，不要求联网，也不新增运行时网络依赖。工作流只会在获得相应权限的范围内读取或写入消费方项目中的材料、计划、`docs/knowledge/` 和计划明确声明的交付路径。

## 数据边界

- 知识产物保留在你的项目目录中，可由你自行用版本控制管理。
- 本技能不会主动把项目内容发送到外部服务，也不会采集使用数据。
- 宿主平台可能依其自身设置联网、进行模型调用或处理数据；这不等同于本技能主动联网。请同时阅读宿主平台的隐私政策和权限提示。
- 外部检索是宿主可选能力，不是本技能运行必需条件；固定验收不应依赖会变化的网络内容。

## 授权边界

普通对话、参数或项目文件不能构成 `trusted_pipeline_signal`。没有经证明的宿主调用元数据时，流程必须回退为普通中文提问并等待确认。发布、发送、登录、删除及其他外部副作用始终需要人工授权。
