# Codex 2.0.0 `zs` 冒烟记录 — 2026-07-16

## 范围

验证 Codex 从消费方 `.agents/skills/` 发现 `zs-*` 并以只读方式运行 `$zs-plan`；不替代完整六阶段、生命周期或评分验收。

## 隔离环境与结果

- 工作区：`/private/tmp/compound-knowledge-codex.Vuo3cJ/`
- 安装目录：六个 `.agents/skills/zs-*` 目录，未保留任何活动 `kw-*` 目录。
- CLI：`codex-cli 0.144.5`，ChatGPT 登录。
- 会话：`019f6b85-4705-7d83-88e5-a559a543d036`
- 输入：`$zs-plan 面向中文 SaaS 团队，制定发布后反馈收集功能的最小计划；先确认范围，不要创建或修改文件。`

Codex 明确回复“按 `zs-plan`”，读取消费方 `AGENTS.md` 与 `zs-plan` 的角色合同，确认没有历史材料，并输出“仅确认范围，不生成计划文件”。运行在只读沙箱中，未创建或修改消费方文件。

## 未验证

中文自然语言自动匹配、其余五个技能、更新、缓存刷新、卸载、`trusted_pipeline_signal`、完整六阶段和回读。
