# WorkBuddy / CodeBuddy 安装与加载证据（2026-07-17）

状态：**部分通过 — 已验证安装与发现，未验证完整工作流。**

## 实际安装

在用户的 macOS 桌面环境运行：

```bash
npx -y skills add oydxxx/compound-knowledge -g -a codebuddy
```

`skills` CLI 从 `https://github.com/oydxxx/compound-knowledge.git` 发现六个技能，并报告已复制到：

- `~/.codebuddy/skills/zs-boom`
- `~/.codebuddy/skills/zs-compound`
- `~/.codebuddy/skills/zs-confidence`
- `~/.codebuddy/skills/zs-plan`
- `~/.codebuddy/skills/zs-review`
- `~/.codebuddy/skills/zs-work`

随后对本机目录的检查确认六份 `SKILL.md` 都存在。

## 加载确认

用户重开 WorkBuddy 后确认：`已加载`。

## 未声称的内容

- 未记录 WorkBuddy 精确版本号；
- 未保存 WorkBuddy 内部截图或原始会话文本；
- 尚未在 WorkBuddy 内完成 `zs-boom` 的完整七步流程。

因此这份材料只能作为一键安装与技能发现的真实证据，不能替代完整 Quick 场景验收。
